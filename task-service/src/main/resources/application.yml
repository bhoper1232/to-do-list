spring:
  application:
    name: task-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5332/tasks_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop